class ClockTester {


    public static void main(String[] args){
        Clock clock1 = new Clock();
        clock1.brand = "Rolex";
        clock1.model = "Submariner";
        clock1.price = 10000.00;
        clock1.color = "Black";
        System.out.println(clock1.brand + "|" + clock1.model + "|" + clock1.price + "|" + clock1.color);

        Clock clock2 = new Clock();
        clock2.brand = "Casio";
        clock2.model = "G-Shock";
        clock2.price = 200.00;
        clock2.color = "Blue";
        System.out.println(clock2.brand + "|" + clock2.model + "|" + clock2.price + "|" + clock2.color);

        Clock clock3 = new Clock();
        clock3.brand = "Fossil";
        clock3.model = "Nate";
        clock3.price = 150.00;
        clock3.color = "Silver";
        System.out.println(clock3.brand + "|" + clock3.model + "|" + clock3.price + "|" + clock3.color);

        Clock clock4 = new Clock();
        clock4.brand = "Seiko";
        clock4.model = "5 Sports";
        clock4.price = 300.00;
        clock4.color = "Green";
        System.out.println(clock4.brand + "|" + clock4.model + "|" + clock4.price + "|" + clock4.color);

        Clock clock5 = new Clock();
        clock5.brand = "Timex";
        clock5.model = "Expedition";
        clock5.price = 50.00;
        clock5.color = "Brown";
        System.out.println(clock5.brand + "|" + clock5.model + "|" + clock5.price + "|" + clock5.color);

        Clock clock6 = new Clock();
        clock6.brand = "Omega";
        clock6.model = "Seamaster";
        clock6.price = 15000.00;
        clock6.color = "Silver";
        System.out.println(clock6.brand + "|" + clock6.model + "|" + clock6.price + "|" + clock6.color);

        Clock clock7 = new Clock();
        clock7.brand = "Swatch";
        clock7.model = "Originals";
        clock7.price = 80.00;
        clock7.color = "Red";
        System.out.println(clock7.brand + "|" + clock7.model + "|" + clock7.price + "|" + clock7.color);

        Clock clock8 = new Clock();
        clock8.brand = "Citizen";
        clock8.model = "Eco-Drive";
        clock8.price = 250.00;
        clock8.color = "Gold";
        System.out.println(clock8.brand + "|" + clock8.model + "|" + clock8.price + "|" + clock8.color);

        Clock clock9 = new Clock();
        clock9.brand = "Bulova";
        clock9.model = "Precisionist";
        clock9.price = 350.00;
        clock9.color = "Black";
        System.out.println(clock9.brand + "|" + clock9.model + "|" + clock9.price + "|" + clock9.color);

        Clock clock10 = new Clock();
        clock10.brand = "Guess";
        clock10.model = "Connect";
        clock10.price = 200.00;
        clock10.color = "Rose Gold";
        System.out.println(clock10.brand + "|" + clock10.model + "|" + clock10.price + "|" + clock10.color);
    }
}
